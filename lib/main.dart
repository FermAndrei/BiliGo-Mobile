import 'package:bilibo/app/app.dart';
import 'package:bilibo/provider/provider.dart';
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';

void main() {
  runApp(
    // ProviderSetup(child:
    // MultiProvider(
    // providers: [ChangeNotifierProvider(create: (_) => ProviderSetup())],
    // child:
    const MyApp(),
    // ),
  );
  // );
}
